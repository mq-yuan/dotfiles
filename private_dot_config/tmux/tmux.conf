# Base tmux configuration. Load fragments in a stable order.
run-shell '\
for f in ~/.config/tmux/conf.d/*.conf \
  ~/.config/tmux/conf.d/keybindings/*.conf \
  ~/.config/tmux/conf.d/hooks/*.conf; do \
  [ -f "$f" ] && tmux source-file "$f"; \
done'
